[project]
name = "rosie-rebirth-kit"
version = "0.1.0"
description = "Portable, auditable rebirth archive scaffolding for Rosie"
requires-python = ">=3.11"
dependencies = ["numpy>=2.0,<3"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
