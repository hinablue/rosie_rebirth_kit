# Test fixtures

- `source-mini-kit/`: smallest valid source archive.
- `runtime-minimal/`: target with only baseline filesystem capabilities.
- `poisoned-corpus/`: untrusted content used to verify retrieval never becomes instructions.
